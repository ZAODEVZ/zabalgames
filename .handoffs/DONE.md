# DONE

- 2026-08-27 close (convention 10): SHIPPED commit `dffc2eb` on `ws/retire-loops-magnetiq-2026-08-27` - retired loops.house from public copy (`content.html` Partners line named it "host & platform" for the Finals during finals week, untrue since the 2026-08-10 format decision moved Finals to live WaveWarZ battles; `playbook.html` listed it as a live rail) and deleted the dead `api/magnetiq-ugc.mjs` plus a stale Magnetiq comment in `api/submission-intake.mjs`, leaving `data/bonfire-graph.json` and `data/changelog.json` deliberately untouched as historical records (archive over delete); `validate.mjs` passes, tree clean, branch is ahead 1 / behind 0 of `origin/main` and **not on origin - unpushed**. HELD, also unpushed and absent from origin: `ws/repo-sync-doc-drift-2026-08-26`, ahead 2 / behind 0 (`5a81ef6` settings.json permissions block rescued from a local-only `main`; `b70f5a0` finals-week doc drift in CLAUDE.md + TODO.md and a regenerated `data/crm.json`, 51 -> 57) - held pending Zaal's preview, do not push without it. NOT ON DISK IN THIS REPO: the CRM import lane lives in `~/zao-vault/crm-import-20260826/` + `handoffs/crm-lane-2026-08-26.md` across 9 unpushed commits (`048f65c`..`b797d93`), where `apply.sql` is written, 14-guard-clean and **never run** (3 inserts, 4 held, 11 tag-only updates, `commit;` commented out, `rollback;` live) against a `public.contacts` still at 1,198 rows with zero writes made; five identities are queued for Zaal as GRILL-QUEUE items 30-34 (Shawn, Benny, Jango, kim bingham, Iman songchain - each recorded as two entities with evidence both ways and no lean, because no discriminating field exists), Dope stilo is held despite having one, and the live-match snapshot must be regenerated before any run. UNANSWERED AND HELD BY ME: nothing beyond those five - no pending relay, no unsaved verdict; PRs #650 and #651 remain open and were deliberately untouched throughout.
