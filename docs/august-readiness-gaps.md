# August-readiness gaps - loops.house Finals spec vs the site

Generated by the overnight audit (2026-07-26). Compares `docs/august-finals-loops-format.md`
(Finals = loops.house + WaveWarZ; July submitters auto-qualify -> 2 weeks of weekly
per-track tasks -> top 2 per track -> WaveWarZ battles -> one winner per track) against
what the site actually supports. The Finals is HOSTED on loops.house, so the site's job is
narrow: qualification, a pointer to loops, and the frozen results. Gaps below.

## Blocking mismatches

- [ ] **`/finals.html` describes the OLD model.** 29 references to WaveWarZ prediction-market
  / 24h build / mentor pairing. The new format is 2 weeks of weekly per-track tasks on
  loops.house then a WaveWarZ battle finale. `/finals` must be rewritten to the loops model
  (or clearly marked "Season 1 Finals runs on loops.house ->" with the new flow). Highest priority.
- [ ] **No loops.house linkage anywhere.** The site never points to the loops.house Finals
  page. Add the loops URL + "how the Finals work" to `/finals` and the homepage August clock.
- [ ] **No qualification surface.** "July submitters auto-qualify" is not shown. Nothing tells
  a builder "you're in the Finals." Options: a badge on `/submissions`, or a `/finals`
  "who qualified" list derived from the July board.

## Missing structure (may live on loops, not the site)

- [ ] **No weekly-task surface.** The 2-week weekly per-track task structure has no page or
  data file. Decide: does loops host the tasks (site just links), or does the site show them?
- [ ] **No "top 2 per track" selection mechanism.** The vote surfaces standings, but nothing
  promotes the top 2 per track into the WaveWarZ finale. Likely a manual/loops step - confirm.
- [ ] **No WaveWarZ battle bracket/surface.** `/finals` references WaveWarZ conceptually but
  there is no battle bracket or link to the live battles. Confirm this lives on WaveWarZ's side.

## Results / finish-line (see docs/season-1-participants.md)

- [ ] **`/winners` needs the per-track winner structure** (artist / builder / creator winner).
  Currently a generic winners page - scaffold it for 3 track winners.
- [ ] **No frozen "Season 1 results" state.** Planned in season-1-participants.md; build the
  data-driven scaffold (empty state fine) so it is ready to populate after Aug 31.

## Likely-dead old-Finals code (verify before removing)

- [ ] `finals-picks.mjs`, `build-vote.mjs`, `builds.mjs`, `register.mjs` + `/enter` register
  flow, `commit-watcher.mjs` (cron retired), `monthly-winner.mjs` (cron retired) all back the
  OLD Finals model (repo registration + prediction-market picks + monthly arcade winner).
  With the Finals on loops.house and voting via `qv-vote`, these are probably dead. Audit each
  for a live consumer; retire the unused ones in a later PR. Do NOT delete blind.

## What is already good

- [x] `/vote` (quadratic, all submissions) is live and correct.
- [x] `/submissions` auto-accept board is live (the qualification source of truth).
- [x] Admin moderation + reset on `/review`.
- [x] `docs/august-finals-loops-format.md` spec exists and is the plan of record.

## Recommended order

1. Rewrite `/finals` to the loops.house model + add the loops link (once RK confirms the loops page).
2. Add a qualification signal (badge or list) from the July board.
3. Scaffold the per-track winners / Season 1 results frozen state.
4. Audit + retire the dead old-Finals endpoints.

## Loose-ends sweep (overnight item 2)

Result: no broken internal links (0 of ~70), no empty/near-empty data files. No
clearly-safe auto-fix found - the items below need a human call, so they are listed not
fixed (per the loop's "don't delete blind" rule).

- **Intentional TODOs (leave):** `finals/live.html` has 7 markers (`0xTODO_WAVEWARZ_BASE`,
  DATA/UI TODO cards) - these are documented placeholders for the WaveWarZ-Base protocol
  Sam owns. Not dead code; they fill in when the protocol ships. Leave until then.
- **Endpoints with no UI reference (12) - verify a consumer before removing:**
  - Legit non-UI (keep): `agent` (agent gateway), `notify`/`webhook`-style, `submission-email`
    (Resend inbound adapter), `triage` (admin repo screen), `empire-leaderboard` (read by the
    empire side), `magnetiq-ugc` (skipped per #497), `win-notify`/`win-drain` (Unlock airdrop path).
  - Retired crons (schedules gone in #572; files kept): `commit-watcher`, `poidh-watcher`
    (Zaal: maybe back later), `workshop-reminders`, `monthly-winner` (Zaal: remove). The last
    two are the safest to delete once their doc/README references are cleaned - flag for a
    dedicated retire-endpoints PR, not a blind delete.
- **Old-Finals endpoints** (`finals-picks`, `build-vote`, `builds`, `register`) - see the
  "likely-dead old-Finals code" section above; same treatment (audit consumers, then retire).
